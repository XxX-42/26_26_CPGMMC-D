# 5. 核心算法

## 5.1 算法思路分析

在第 4 章中，本文建立了统一的无人机调度基本模型。该模型给出了无人机在给定任务序列下的飞行时间、载荷变化、能源消耗、SOC 演化以及通信链路状态，这些物理关系需要通过程序化算法转化为反复调用的计算模块。问题一、二、三、四的不同决策均建立在相同的物理基础上，核心算法依次执行地理预计算、任务状态递推、能源状态更新、三维轨迹重建和通信区间认证。

其中，地形与航段几何只与节点位置有关，可提前计算并缓存；而载荷、能源和通信状态随具体任务变化，则在任务执行过程中逐阶段更新。

整个流程先从节点坐标、DEM、设备参数和通信参数出发，依次完成航段地形与几何预处理、飞行时间与逐段载荷更新、能耗与 SOC 及资源再次可用时间计算、连续三维轨迹重建、DEM 遮挡与双向链路预算，最终得到可行性结果。

## 5.2 DEM 航段预计算算法

### 5.2.1 算法基本思想

对任意两个任务节点 \(i,j\)，第 4 章已经得到水平距离

\[
d_{ij}
=
\sqrt{(x_i-x_j)^2+(y_i-y_j)^2},
\tag{5-1}
\]

以及航段巡航海拔

\[
H_{ij}^{c}
=
H_{ij}^{\max}+50.
\tag{5-2}
\]

为了准确获得

\[
H_{ij}^{\max}.
\]

避免对直线进行稀疏等距采样，有可能恰好跳过狭窄山脊，导致巡航高度被低估的问题，我们直接确定水平航迹穿过的全部 DEM 栅格。

设航段在平面上的参数方程为

\[
\begin{cases}
x(\lambda)
=
x_i+\lambda(x_j-x_i),\\[3pt]
y(\lambda)
=
y_i+\lambda(y_j-y_i),
\end{cases}
\qquad
0\le\lambda\le1.
\tag{5-3}
\]

随着 \(\lambda\) 从 0 增加到 1，航迹依次穿过若干 DEM 像元。记其集合为

\[
\mathcal C_{ij}
=
\{c_1,c_2,\ldots,c_N\}.
\tag{5-4}
\]

依次读取各像元的原始 DEM 高程，则有

\[
H_{ij}^{\max}
=
\max_{1\le r\le N}H_{c_r}.
\tag{5-5}
\]

随后利用式（5-2）得到巡航海拔，并进一步计算

\[
h_{ij}^{+}
=
H_{ij}^{c}-H_i^{op},
\tag{5-6}
\]

\[
h_{ij}^{-}
=
H_{ij}^{c}-H_j^{op}.
\tag{5-7}
\]

这样，一个有向航段最终被预处理成

\[

\Gamma_{ij}
=
(
d_{ij},
\mathcal C_{ij},
H_{ij}^{\max},
H_{ij}^{c},
h_{ij}^{+},
h_{ij}^{-}
)

\tag{5-8}
\]

的标准航段对象。

### 5.2.2 正反向一致性检验

由于

\[
i\rightarrow j
\]

与

\[
j\rightarrow i
\]

在水平面上经过的是同一直线，因此二者的 DEM 像元集合应满足

\[
\mathcal C_{ij}
=
\mathcal C_{ji},
\tag{5-9}
\]

从而有

\[
H_{ij}^{\max}
=
H_{ji}^{\max}.
\tag{5-10}
\]

由于起点与终点不同，其爬升和下降高度通常满足

\[
h_{ij}^{+}\neq h_{ji}^{+},
\qquad
h_{ij}^{-}\neq h_{ji}^{-}.
\tag{5-11}
\]

这样能在缓存对称的地形信息的基础上，保留两个方向的爬降参数，防止把一个航段的全部属性当作无向量处理。

### 5.2.3 DEM 航段算法伪代码

**算法 1：DEM 约束下的航段预计算**

```text
输入：
    节点 i、j 的坐标与作业高度
    原始 DEM 栅格

输出：
    航段对象 Γij

1. 将节点 i、j 转换至统一米制平面坐标；
2. 计算水平距离 dij；
3. 构造线段 PiPj；
4. 求线段穿过的全部 DEM 像元集合 Cij；
5. 若任一关键像元为 NoData 或越界：
       返回 GEOMETRY_INVALID；
6. 读取 Cij 中全部原始高程；
7. Hmax ← max(Hc, c∈Cij)；
8. Hc ← Hmax + 50；
9. hup ← Hc - Hop_i；
10. hdown ← Hc - Hop_j；
11. 若 hup<0 或 hdown<0：
       返回 GEOMETRY_CONFLICT；
12. 保存：
       dij, Cij, Hmax, Hc, hup, hdown；
13. 返回 Γij。
```

该算法只对固定节点之间的几何关系执行一次，后续任务均直接复用航段缓存，因此能够显著减少重复 DEM 查询。

## 5.3 飞行—载荷—能耗递推算法

### 5.3.1 飞行阶段状态更新

对航段 \(i\rightarrow j\)，根据第 4 章建立的三阶段飞行模型，其飞行时间为

\[
t_{gij}
=
\frac{h_{ij}^{+}}{v_g^{\uparrow}}
+
\frac{d_{ij}}{v_g^c}
+
\frac{h_{ij}^{-}}{v_g^{\downarrow}}.
\tag{5-12}
\]

因此每个航段可以拆分为三个连续事件：

\[

\text{CLIMB}
\rightarrow
\text{CRUISE}
\rightarrow
\text{DESCEND}

\tag{5-13}
\]

若节点处存在物资交接，则在下降后进一步产生

\[
\text{SERVICE}
\]

事件。

于是，一个任务的全过程通常经历准备、爬升、巡航、下降和服务等阶段；进入后续航段后重复爬升、巡航、下降及必要的服务过程，最终返回。

### 5.3.2 载荷状态递推

设任务访问序列为

\[
(v_0,v_1,\ldots,v_m,v_{m+1}),
\]

其中

\[
v_0=v_{m+1}=O.
\]

任务起始载荷为

\[
q_0
=
\sum_{r=1}^{m}W_{v_r}.
\tag{5-14}
\]

完成第 \(r\) 个投送节点后，有

\[
q_r
=
q_{r-1}-W_{v_r}.
\tag{5-15}
\]

因此

\[
q_0
\ge q_1
\ge\cdots
\ge q_m=0.
\tag{5-16}
\]

于是，第 \(r\) 个航段实际采用的载荷为

\[
q_r,
\]

第 4 章所用能耗函数是载荷 \(q\) 的函数，因此必须先更新任务状态，再调用能耗公式。

### 5.3.3 逐段能耗递推

已知载荷相关航程

\[
L_g(q)
=
L_g^0
-
(L_g^0-L_g^F)
\left(\frac{q}{Q_g}\right)^{3/2},
\tag{5-17}
\]

则水平飞行能耗为

\[
E_{gij}^{hor}(q)
=
E_g^{use}
\frac{d_{ij}}{L_g(q)},
\tag{5-18}
\]

爬升附加能耗为

\[
E_{gij}^{up}(q)
=
\frac{(m_g^0+q)gh_{ij}^{+}}
{3.6\times10^6\eta_g^{up}}.
\tag{5-19}
\]

故当前航段能耗为

\[
E_r
=
E_{g,v_rv_{r+1}}(q_r)
=
E_{g,v_rv_{r+1}}^{hor}(q_r)
+
E_{g,v_rv_{r+1}}^{up}(q_r).
\tag{5-20}
\]

定义累计能耗

\[
E^{(r)}
=
\sum_{\ell=0}^{r}E_\ell.
\tag{5-21}
\]

则可以递推写成

\[

E^{(r)}
=
E^{(r-1)}
+
E_{g,v_rv_{r+1}}(q_r)

\tag{5-22}
\]

其初值为

\[
E^{(-1)}=0.
\]

任务结束后即得到总能耗

\[
E^{T}
=
E^{(m)}.
\tag{5-23}
\]

### 5.3.4 飞行—能耗递推算法伪代码

**算法 2：逐段载荷更新与运输能耗计算**

```text
输入：
    任务访问序列 V=(v0,...,vm+1)
    各节点投送货物
    机型 g
    航段缓存 Γ

输出：
    完整时间事件
    总能耗 ET
    任务结束 SOC

1. 计算任务起飞总载荷 q；
2. 若 q > Qg：
       返回 PAYLOAD_INFEASIBLE；
3. 初始化：
       E_total ← 0
       time ← 0
4. 对 r = 0,...,m：
       i ← vr
       j ← vr+1

       读取航段缓存 Γij；

       根据当前载荷 q 计算：
           Lg(q)
           Ehor_gij(q)
           Eup_gij(q)
           Egij(q)

       计算：
           t_up
           t_cruise
           t_down

       将 CLIMB、CRUISE、DESCEND
       三阶段加入时间轴；

       E_total ← E_total + Egij(q)

       若 j 为投送节点：
           加入 SERVICE 事件；
           q ← q - 当前节点投送质量；

5. 检查最终 q = 0；
6. 计算：
       SOC_end = 1 - E_total / Euse_g；
7. 若 SOC_end < ρg：
       返回 ENERGY_INFEASIBLE；
8. 返回完整任务状态。
```

这样可以保证时间、载荷、能耗三类变量沿同一条任务时间轴同步演化。

## 5.4 SOC—充电—资源状态更新算法

## 5.4.1 能源状态机

对于任一共享电池或能源组件，本文采用如下状态机：

能源组件的状态依次为 FULL、IN\_USE 和 CHARGING，并在充电完成后回到 FULL。

其中，`FULL`：SOC 为 \(100\%\)，可执行新任务，`IN_USE`：正在随无人机执行任务，`CHARGING`：任务结束后正在恢复 SOC。

### 5.4.2 任务后 SOC 更新

设资源任务前满电，任务实际消耗能量为 \(E\)，则结束 SOC 为

\[
s
=
1-\frac{E}{E^{use}}.
\tag{5-24}
\]

根据两阶段充电模型，

\[
t^{chg}(s)=
\begin{cases}
T^{full}
\left[
0.65\dfrac{0.90-s}{0.90}+0.35
\right],
&0\le s<0.90,\\[8pt]
T^{full}
\left[
0.35\dfrac{1-s}{0.10}
\right],
&0.90\le s\le1.
\end{cases}
\tag{5-25}
\]

若任务结束时刻为 \(t^{end}\)，则资源再次满电的时刻为

\[
t^{full}
=
t^{end}
+
t^{chg}(s).
\tag{5-26}
\]

所以该能源资源完整不可复用区间为

\[
I^B
=
[t^{start},t^{full}).
\tag{5-27}
\]

### 5.4.3 事件驱动的资源检查

对于某一具体资源，将其全部任务按照开始时刻排序：

\[
(s_1,e_1^{full}),
(s_2,e_2^{full}),
\ldots .
\]

由于要求

\[
s_{r+1}
\ge
e_r^{full}.
\tag{5-28}
\]

无需在整个时间轴遍历检查资源，只需比较相邻事件边界。若在同一时刻存在释放和占用事件，则按先释放，后占用进行处理，对应半开区间

\[
[s,e).
\]

能够避免任务恰好首尾衔接时产生资源冲突。

### 5.4.4 能源资源更新伪代码

**算法 3：能源资源状态递推**

```text
输入：
    任务开始时间 t_start
    任务结束时间 t_end
    任务能耗 E
    可用能量 E_use
    完全充电时间 T_full
    当前资源状态

输出：
    SOC_end
    再次可用时刻 t_full

1. 若资源当前状态 ≠ FULL：
       返回 RESOURCE_BUSY；

2. 状态 ← IN_USE；

3. SOC_end ← 1 - E / E_use；

4. 若 SOC_end < 返航安全阈值：
       返回 ENERGY_INFEASIBLE；

5. 到达任务结束时刻：
       状态 ← CHARGING；

6. 根据 SOC_end：
       调用两阶段充电函数 t_chg(SOC_end)；

7. t_full ← t_end + t_chg；

8. 在 t_full：
       状态 ← FULL；

9. 返回 SOC_end 和 t_full。
```

## 5.5 通信链路快速判定算法

### 5.5.1 从链路预算推导距离界

从第 4 章建立的链路可用条件可知

\[
L_{ab}^{tot}(t)
\le
L_{ab}^{bi}.
\tag{5-29}
\]

其中自由空间损耗为

\[
FSPL
=
32.45
+
20\log_{10}f_{\rm MHz}
+
20\log_{10}D_{\rm km}.
\tag{5-30}
\]

若无遮挡，

\[
b=0,
\]

临界可用状态满足

\[
32.45
+
20\log_{10}f
+
20\log_{10}D
=
L^{bi}.
\]

移项得

\[
20\log_{10}D
=
L^{bi}
-
32.45
-
20\log_{10}f,
\]

因此

\[

R_{\rm clear}
=
10^{
\frac{
L^{bi}-32.45-20\log_{10}f
}{20}
}

\tag{5-31}
\]

表示无遮挡情况下的最大理论通信距离。

若存在地形遮挡，需要增加 \(L_{obs}\)，于是

\[
32.45
+
20\log_{10}f
+
20\log_{10}D
+
L_{obs}
=
L^{bi}.
\]

可得

\[
R_{\rm blocked}
=
10^{
\frac{
L^{bi}-L_{obs}-32.45-20\log_{10}f
}{20}
}.
\tag{5-32}
\]

与式（5-31）比较，

\[

R_{\rm blocked}
=
R_{\rm clear}
10^{-L_{obs}/20}

\tag{5-33}
\]

显然有

\[
R_{\rm blocked}
<
R_{\rm clear}.
\tag{5-34}
\]

由此得到三个快速判定区间：

\[
D\le R_{\rm blocked}
\]

时，即使发生遮挡也仍满足通信预算，因此无需再计算 DEM 遮挡；

当

\[
D>R_{\rm clear},
\]

即使无遮挡也无法满足通信预算；

只有

\[
R_{\rm blocked}
<
D
\le
R_{\rm clear}
\tag{5-35}
\]

时，才执行 DEM 视线检查，可显著减少重复地形计算。

### 5.5.2 单时刻链路判定

对任意时刻 \(t\)，算法依次执行：

单时刻链路判定依次检查三维距离、距离界预筛、DEM 遮挡、传播损耗和通信余量。

实际链路余量为

\[
M_{ab}(t)
=
L_{ab}^{bi}
-
L_{ab}^{tot}(t).
\tag{5-36}
\]

最终

\[
A_{ab}(t)
=
\begin{cases}
1,&M_{ab}(t)\ge0,\\
0,&M_{ab}(t)<0.
\end{cases}
\tag{5-37}
\]

若采用两跳中继，任意一跳失效都会导致整个中继链路失效，则总通信余量取两跳中的较小值：

\[

M^{relay}(t)
=
\min
\left\{
M_{TR}(t),
M_{RG}(t)
\right\}

\tag{5-38}
\]

## 5.6 连续通信自适应认证算法

### 5.6.1 步长采样

如果按固定步长进行采样，每两个相邻样点之间仍可能存在短暂的地形遮挡或链路预算失效。因此所有离散采样点可用不能严格推出整个连续区间可用。连续通信认证采用阶段边界检查、区间保守认证、递归细分和加密采样复核相结合的方法。

### 5.6.2 运动区间模型

在一个确定的飞行阶段内，无人机沿直线运动。

设时间区间为

\[
[a,b],
\]

其位置满足

\[
\mathbf x(t)
=
\mathbf x(a)
+
\theta(t)
\left[
\mathbf x(b)-\mathbf x(a)
\right],
\qquad
0\le\theta\le1.
\tag{5-39}
\]

对于固定通信端点 \(r\)，该区间距离的一个保守上界为

\[
D_{\max}
=
\max
\left\{
\|\mathbf x(a)-r\|,
\|\mathbf x(b)-r\|
\right\}.
\tag{5-40}
\]

若按照最坏情况，整个区间都受到地形遮挡，有

\[
FSPL(D_{\max})
+
L_{obs}
\le
L^{bi},
\tag{5-41}
\]

则无论区间内部具体遮挡状态如何，均有

\[
M(t)\ge0,
\qquad
\forall t\in[a,b].
\]

因此该区间可直接标记为\[\text{CERTIFIED}\]

而无需继续采样。


### 5.6.3 无遮挡区间

如果式（5-41）不成立，但

\[
FSPL(D_{\max})
\le
L^{bi},
\tag{5-42}
\]

则该区间是否可用取决于地形遮挡。

固定端点 \(r\) 与移动无人机在

\[
[a,b]
\]

内的全部视线组成一个连续扫掠区域。因此算法检查该区域与 DEM 地形是否可能相交。

若能够证明整个区间无遮挡，则采用

\[
b=0
\]

重新计算最坏传播损耗；若仍满足预算，则该区间被认证为可用。

若无法直接证明，则把区间二分：

\[
[a,b]
\rightarrow
[a,m]\cup[m,b],
\]

其中

\[
m=\frac{a+b}{2},
\tag{5-43}
\]

然后分别递归判断。

### 5.6.4 未认证区间

如果区间已经缩小至设定的空间或时间精度，但仍无法证明其连续可用，则不能按照采样结果把它视为安全，而标记为

\[
\text{UNCERTIFIED}
\]

正式可行结果要求

\[
T_{\rm uncertified}=0
\]

能够防止因离散采样造成的“假连续通信”。

### 5.6.5 自适应区间认证伪代码

**算法 4：连续通信区间递归认证**

```text
函数 CERTIFY(a, b, endpoint):

输入：
    运输无人机在 [a,b] 的线性轨迹
    固定网关或悬停中继 endpoint
    双向允许损耗 L_bi
    DEM

输出：
    CERTIFIED / OUTAGE / UNCERTIFIED
    最小认证通信余量

1. 计算区间端点三维位置；
2. 计算距离上界 D_max；

3. 若
       FSPL(D_max) + L_obs <= L_bi：
       返回 CERTIFIED；

4. 若区间能够整体证明无遮挡 且
       FSPL(D_max) <= L_bi：
       返回 CERTIFIED；

5. 计算中点 m=(a+b)/2；
6. 对 a、m、b 执行精确链路计算；

7. 若发现明确通信失效：
       返回 OUTAGE；

8. 若区间长度 > 最小允许尺度：
       left  ← CERTIFY(a,m)
       right ← CERTIFY(m,b)

       若 left、right 均 CERTIFIED：
            返回 CERTIFIED；
       若任一 OUTAGE：
            返回 OUTAGE；
       否则：
            返回 UNCERTIFIED；

9. 若达到最小尺度仍不能证明：
       返回 UNCERTIFIED。
```

## 5.7 统一算法

将上述算法封装为任务评估接口。

设任务对象为 \(\Pi\)，其包含：

\[
\Pi
=
(
\text{节点序列},
\text{设备类型},
\text{载荷状态},
\text{开始时刻},
\text{资源对象}
).
\tag{5-44}
\]

统一评估器输出

\[
\Phi(\Pi)
=
(
T,
E,
SOC,
\mathcal I,
\mathbf x(t),
\mathcal C(t),
F
),
\tag{5-45}
\]

其中，\(T\)：任务各阶段时刻，\(E\)：任务总能耗，\(SOC\)：能源状态，\(\mathcal I\)：设备及能源资源占用区间，\(\mathbf x(t)\)：连续三维轨迹，\(\mathcal C(t)\)：通信状态， \(F\)：物理可行性标志。

### 5.7.1 统一算法伪代码

**算法 5：无人机任务统一物理评估算法**

```text
输入：
    原始节点与 DEM
    设备参数
    能源参数
    通信参数
    给定任务描述 Π

输出：
    标准任务评估结果 Φ(Π)

阶段 A：地理预处理
1. 对任务涉及的每一条航段：
       调用算法1；
       获得距离、DEM最高点、巡航高度及爬降高度；

阶段 B：飞行与载荷递推
2. 初始化任务载荷、累计时间与累计能耗；
3. 依次遍历各航段：
       更新当前载荷；
       计算三阶段飞行时间；
       计算水平及爬升能耗；
       写入任务时间轴；

阶段 C：能源更新
4. 计算任务结束 SOC；
5. 检查返航安全余量；
6. 调用算法3计算充电时间及资源再次可用时刻；

阶段 D：三维轨迹重建
7. 根据各阶段起止位置和时间：
       建立连续轨迹 x(t)；

阶段 E：通信计算
8. 对全部需要通信保障的阶段：
       计算三维距离；
       计算双向链路预算；
       调用算法4进行连续区间认证；

阶段 F：综合可行性
9. 检查：
       质量；
       体积；
       能源；
       资源时间；
       连续通信；
10. 若所有条件成立：
       F ← TRUE；
    否则：
       F ← FALSE，并记录失败类型；

11. 返回 Φ(Π)。
```

## 5.8 模型与算法的联系

第 4 章的基本模型主要确定每个任务在给定状态下的计算条件，本章的算法按照任务执行顺序调用这些公式，并保证状态之间正确衔接。二者之间的对应关系可以表示为：

| 基本模型 | 核心算法 | 算法作用 |
|---|---|---|
| DEM 三维飞行模型 | 航段栅格预计算 | 将连续地形转换成标准航段对象 |
| 载荷相关航程模型 | 载荷递推算法 | 投送后逐段更新剩余载荷 |
| 运输能耗模型 | 逐航段能耗累计 | 计算真实任务总能耗 |
| SOC 模型 | 能源状态更新 | 将任务能耗映射为 SOC |
| 两阶段充电模型 | 事件驱动资源算法 | 得到资源再次可用时刻 |
| 三维链路模型 | 链路快速判定 | 判断单时刻通信状态 |
| 连续通信模型 | 自适应区间认证 | 排除离散采样造成的漏检 |
| 资源占用模型 | 区间冲突检查 | 验证实体资源时间可行性 |
